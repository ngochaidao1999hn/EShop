@model IEnumerable<EShop.Models.OrderDetails>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Detail_Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Orders.Ord_CreateDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Pro_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Detail_Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Orders.Ord_CreateDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Pro_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Detail_PriceEach }) |
            @Html.ActionLink("Details", "Details", new { id=item.Detail_PriceEach }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Detail_PriceEach })
        </td>
    </tr>
}

</table>

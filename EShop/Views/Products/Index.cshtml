@model IEnumerable<EShop.Models.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categories.Cate_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categories.Cate_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pro_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pro_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pro_Id })
        </td>
    </tr>
}

</table>

@model IEnumerable<EShop.Models.Reviews>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Review_DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Review_Content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Review_Rate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customers.Cus_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Pro_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Review_DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Review_Content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Review_Rate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customers.Cus_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Pro_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Review_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Review_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Review_Id })
        </td>
    </tr>
}

</table>

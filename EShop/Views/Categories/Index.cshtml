@model IEnumerable<EShop.Models.Categories>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cate_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cate_Image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cate_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cate_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cate_Image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cate_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cate_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cate_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cate_Id })
        </td>
    </tr>
}

</table>
